import styled from "styled-components";

export const Button = styled.button`
  border: none;
  background-color: transparent;
  color: inherit;
  line-height: normal;
  outline: none;
  box-shadow: none;
  font-family: inherit;
  font-size: inherit;
  cursor: pointer;
`;